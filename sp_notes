uvicorn worktrace_api.main:app --app-dir apps/api/src --reload

http://127.0.0.1:8000/docs



'/Users/pratap/Library/Application Support/electron-app'

REGRESSSSION FIXED BY VISHAL0



docker compose up        # everything (api + worker + redis)
docker compose up -d     # detached
docker compose logs -f worker
docker compose down      # data (sqlite + recordings) is bind-mounted on host, so it survives